import 'package:flutter/material.dart';
import 'package:vacstage_flutter/layouts/select.dart';

class SelectPage extends StatelessWidget {
  @override 
  Widget build(BuildContext context) {
    return Container(
      child: Select(),
    );
  }
}